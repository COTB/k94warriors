@{
    Layout = null;
}
<nav class="navbar navbar-inverse" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Return to Dog List", "Index", "Dog", null, new { @class = "navbar-brand" })
            @Html.ActionLink("View Profile", "DogProfile", "Dog", new { id = ViewBag.DogId}, new { @class = "navbar-brand"} )
            @*            <a class="navbar-brand" href="/dog/readdog/@ViewBag.DogId">View @ViewBag.DogName</a>*@
        </div>

        <div class="collapse navbar-collapse navbar-ex1-collapse">
            <ul class="nav navbar-nav">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">@ViewBag.DogName's Notes <b class="caret"></b></a>
                    <ul class="dropdown-menu">
                        <li>@Html.ActionLink("View Notes", "Index", "Notes", new { dogProfileId = ViewBag.DogId }, null)</li>
                        <li>@Html.ActionLink("Add Notes", "Create", "Notes", new { dogId = ViewBag.DogId }, null)</li>
                    </ul>
                </li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">@ViewBag.DogName's Medical Records <b class="caret"></b></a>
                    <ul class="dropdown-menu">
                        <li>@Html.ActionLink("View Medical Records", "Index", "MedicalRecords", new { dogProfileId = ViewBag.DogId }, null)</li>
                        <li>@Html.ActionLink("Add Medical Record", "Create", "MedicalRecords", new { dogProfileId = ViewBag.DogId }, null)</li>
                    </ul>
                </li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">@ViewBag.DogName's Events <b class="caret"></b></a>
                    <ul class="dropdown-menu">
                        <li>@Html.ActionLink("View Events", "Index", "Events", new { dogId = ViewBag.DogId }, null)</li>
                        <li>@Html.ActionLink("Add Event", "Create", "Events", new { dogId = ViewBag.DogId }, null)</li>
                    </ul>
                </li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
</nav>