@model K94Warriors.Models.MedicalRecordType
@using K94Warriors.Core.HtmlHelperExtensions

@{
    ViewBag.Title = "Edit Medical Record Type " + Model.Name;
}

<h2>Edit Medical Record Type</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    @Html.HiddenFor(i => i.MedicalRecordTypeID)

    <div class="form-group">
        @Html.LabelFor(model => model.Name)
        @Html.K9TextBoxFor(model => model.Name, "Medical Record Type Name")
        @Html.ValidationMessageFor(model => model.Name)
    </div>
        
    <p>
        <input class="btn" type="submit" value="Save Changes" />
    </p>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
